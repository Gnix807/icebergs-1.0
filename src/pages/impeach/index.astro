---
import Layout from '../../layouts/Layout.astro';
import { prisma } from '../../lib/prisma';
import FeatureGate from '../../components/FeatureGate.astro';

const openRequests = await prisma.impeachRequest.findMany({
  where: { status: 'OPEN' },
  orderBy: { createdAt: 'desc' },
  select: {
    id: true, targetRole: true, reason: true,
    createdAt: true, closesAt: true,
    target:    { select: { id: true, username: true, nickname: true } },
    initiator: { select: { id: true, username: true, nickname: true } },
    _count: { select: { votes: true } },
  },
});

const historyRequests = await prisma.impeachRequest.findMany({
  where: { status: { in: ['PASSED', 'REJECTED', 'CANCELLED'] } },
  orderBy: { resolvedAt: 'desc' },
  take: 20,
  select: {
    id: true, targetRole: true, status: true,
    createdAt: true, resolvedAt: true,
    target:    { select: { id: true, username: true, nickname: true } },
    initiator: { select: { id: true, username: true, nickname: true } },
    _count: { select: { votes: true } },
  },
});

const STATUS_LABEL: Record<string, string> = {
  OPEN:      '投票中',
  PASSED:    '已通过',
  REJECTED:  '未通过',
  CANCELLED: '已撤销',
};
const STATUS_COLOR: Record<string, string> = {
  OPEN:      '#ef4444',
  PASSED:    '#8b5cf6',
  REJECTED:  '#22c55e',
  CANCELLED: '#6b7280',
};
---

<Layout title="弹劾案 — 冰山图宇宙">
  <FeatureGate feature="feature_impeach" name="弹劾流程">
  <main class="max-w-2xl mx-auto px-4 py-8 reveal">

    <div class="text-[10px] font-mono text-text-lo mb-6">
      <a href="/org" class="hover:text-text-body transition-colors">机构</a>
      <span class="mx-2">›</span>
      <span>弹劾案</span>
    </div>

    <div class="mb-8">
      <h1 class="text-xl font-mono text-text-hi mb-1">弹劾案记录</h1>
      <p class="text-[11px] font-mono text-text-lo">ADMIN/FOUNDER 可对 MODERATOR 发起弹劾；FOUNDER 可对 ADMIN 发起弹劾。通过后目标角色降级。</p>
    </div>

    <!-- 进行中 -->
    <section class="mb-10">
      <div class="flex items-center gap-2 mb-4">
        <span class="text-[10px] font-mono text-danger tracking-widest">// 进行中的弹劾案</span>
        <div class="flex-1 border-t border-border-minimal"></div>
        <span class="text-[10px] font-mono text-text-lo">{openRequests.length} 项</span>
      </div>

      {openRequests.length === 0 ? (
        <div class="py-10 text-center border border-dashed border-border-minimal">
          <p class="text-[10px] font-mono text-text-lo">// 目前无进行中的弹劾案</p>
        </div>
      ) : (
        <div class="space-y-3">
          {openRequests.map((req: any) => {
            const daysLeft = Math.max(0, Math.ceil((new Date(req.closesAt).getTime() - Date.now()) / 86400_000));
            return (
              <a href={`/impeach/${req.id}`}
                class="block border border-danger/20 bg-[#ef444405] hover:bg-[#ef44440d] transition-colors p-4 group">
                <div class="flex items-start justify-between gap-3">
                  <div class="flex-1 min-w-0">
                    <div class="flex items-center gap-2 flex-wrap mb-1">
                      <span class="text-[10px] font-mono border px-1.5 py-0.5"
                        style={`color:#ef4444;border-color:#ef444440`}>
                        弹劾 {req.targetRole}
                      </span>
                      <a href={`/user/${req.target.id}`}
                        class="text-sm font-mono text-text-hi group-hover:text-danger transition-colors"
                        onclick="event.stopPropagation()">
                        @{req.target.nickname ?? req.target.username}
                      </a>
                    </div>
                    <p class="text-[11px] font-mono text-text-lo line-clamp-1">{req.reason}</p>
                    <div class="text-[10px] font-mono text-text-lo mt-1.5">
                      发起人：@{req.initiator.nickname ?? req.initiator.username}
                      · {req._count.votes} 票 · 剩余 {daysLeft} 天
                    </div>
                  </div>
                  <span class="text-[10px] font-mono border border-danger/25 text-danger px-1.5 py-0.5 flex-shrink-0">
                    投票中
                  </span>
                </div>
              </a>
            );
          })}
        </div>
      )}
    </section>

    <!-- 历史 -->
    <section>
      <div class="flex items-center gap-2 mb-4">
        <span class="text-[10px] font-mono text-text-lo tracking-widest">// 历史记录</span>
        <div class="flex-1 border-t border-border-minimal"></div>
      </div>

      {historyRequests.length === 0 ? (
        <div class="text-[10px] font-mono text-text-lo py-4 text-center">// 暂无历史</div>
      ) : (
        <div class="space-y-2">
          {historyRequests.map((req: any) => (
            <a href={`/impeach/${req.id}`}
              class="flex items-center gap-3 px-4 py-3 border border-border-minimal hover:border-border transition-colors group">
              <div class="flex-1 min-w-0">
                <div class="flex items-center gap-2 flex-wrap">
                  <span class="text-[10px] font-mono"
                    style={`color:${STATUS_COLOR[req.status]}`}>
                    {STATUS_LABEL[req.status]}
                  </span>
                  <span class="text-xs font-mono text-text-body group-hover:text-text-hi transition-colors">
                    @{req.target.nickname ?? req.target.username}
                  </span>
                  <span class="text-[10px] font-mono text-text-lo">({req.targetRole})</span>
                </div>
              </div>
              <span class="text-[10px] font-mono text-text-lo flex-shrink-0">
                {req.resolvedAt ? new Date(req.resolvedAt).toLocaleDateString('zh-CN') : '—'}
              </span>
            </a>
          ))}
        </div>
      )}
    </section>

  </main>
  </FeatureGate>
</Layout>
