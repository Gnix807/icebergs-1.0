---
import Layout from '../layouts/Layout.astro';
import { LABEL_DEFS } from '../lib/labels';
import { ICEBERG_TOPICS } from '../lib/icebergTopic';

const labelCount = LABEL_DEFS.length;
const topicCount = ICEBERG_TOPICS.length;
---

<Layout
  title="平台规则 — 冰山图宇宙"
  description="冰山图宇宙的内容发布、多人协作、站务权限与成就规则。"
>
  <main class="mx-auto max-w-5xl px-4 py-8 sm:py-12">
    <header class="mb-8 border-b border-border pb-6">
      <p class="mb-2 font-mono text-[10px] tracking-[0.24em] text-brand">GOVERNANCE / RULES</p>
      <h1 class="font-mono text-2xl font-semibold text-text-hi sm:text-3xl">平台规则与协作公约</h1>
      <p class="mt-3 max-w-3xl text-sm leading-7 text-text-body">
        这页说明内容怎样发布、多人怎样一起改稿，以及哪些站务需要专门授权。
        贡献和成就会留在个人主页，但不会自动换来审核或管理权限。
      </p>
      <div class="mt-4 flex flex-wrap gap-2 font-mono text-[10px]">
        <a class="rule-chip" href="#content">内容</a>
        <a class="rule-chip" href="#collaboration">版本协作</a>
        <a class="rule-chip" href="#capabilities">能力认证</a>
        <a class="rule-chip" href="#contributions">贡献与成就</a>
        <a class="rule-chip" href="#governance">治理与申诉</a>
        <a class="rule-chip" href="#legacy">历史制度</a>
      </div>
    </header>

    <section class="rule-callout mb-8">
      <div class="font-mono text-xs font-semibold text-brand">先记住两件事</div>
      <div class="mt-3 grid gap-3 sm:grid-cols-2">
        <p><strong>提案不等于写入权限。</strong>外部用户可以提交自己的分支和合并请求，但不能直接改写主版本。</p>
        <p><strong>成就不等于能力。</strong>分数、等级、徽章和成就永远不能授予审核、管理或站点权限。</p>
      </div>
    </section>

    <section id="content" class="rule-section">
      <div class="rule-heading">
        <span>01</span>
        <div><h2>内容与发布</h2><p>写完草稿后，需要通过审核才会公开。</p></div>
      </div>
      <div class="rule-grid">
        <article class="rule-panel">
          <h3>冰山图基本要求</h3>
          <ul>
            <li>标题、简介、层级和词条应清楚描述同一主题，避免无意义堆砌。</li>
            <li>引用事实、争议说法或敏感信息时，应尽可能写明来源和不确定性。</li>
            <li>不得发布违法内容、恶意人肉、骚扰、仇恨或有现实伤害风险的指引。</li>
            <li>平台提供 {topicCount} 个主题和 {labelCount} 个标签；标签用于告知读者，不用于规避审核。</li>
          </ul>
        </article>
        <article class="rule-panel">
          <h3>合并不等于发布</h3>
          <ul>
            <li>主版本保存最新草稿，公开页面仍显示上一次审核通过的内容。</li>
            <li>合并 PR 不会立即改变公开页面，仍需提交发布审核。</li>
            <li>审核期间可以继续修改草稿；审核结果只对应当时送审的版本。</li>
            <li>内容需要下架时，提交记录和讨论仍会保留，方便以后核对。</li>
          </ul>
        </article>
      </div>
    </section>

    <section id="collaboration" class="rule-section">
      <div class="rule-heading">
        <span>02</span>
        <div><h2>版本协作与专题</h2><p>专题负责分工，版本记录负责保存每次改稿。</p></div>
      </div>
      <div class="rule-grid sm:grid-cols-3">
        <article class="rule-panel">
          <h3>维护者 MAINTAINER</h3>
          <p>可以管理主版本、协作者和合并请求。通常是冰山图作者、专题创建者或专题管理员。</p>
        </article>
        <article class="rule-panel">
          <h3>贡献者 CONTRIBUTOR</h3>
          <p>可以在自己的分支上修改并提交 PR，但不能直接改主版本，也不能管理其他协作者。</p>
        </article>
        <article class="rule-panel">
          <h3>提案者 PROPOSER</h3>
          <p>可以为开放投稿的冰山图提修改，但只能操作自己的分支和 PR。</p>
        </article>
      </div>
      <div class="rule-panel mt-3">
        <h3>外部提案默认规则</h3>
        <ul class="sm:columns-2">
          <li>公开个人仓库默认开放提案；私有仓库与 Projects 仓库默认邀请制。</li>
          <li>每张冰山图最多 3 个未关闭外部 PR。</li>
          <li>每名用户全站最多 10 个未关闭外部 PR，每日最多新建 10 个。</li>
          <li>维护者可以关闭提案或屏蔽特定提案者，但历史记录必须保留。</li>
          <li>自动保存只保存在自己的工作区；点击“提交版本”后，其他人才会看到这次改动。</li>
          <li>恢复旧内容会新增一条恢复记录，不会抹掉中间发生过的修改。</li>
        </ul>
      </div>
    </section>

    <section id="capabilities" class="rule-section">
      <div class="rule-heading">
        <span>03</span>
        <div><h2>站务权限</h2><p>站务按工作内容分别授权，不会附带别人的作品编辑权。</p></div>
      </div>
      <div class="rule-grid sm:grid-cols-2">
        <article class="rule-panel"><h3 class="text-success">发布审核 PUBLICATION_REVIEW</h3><p>检查准备公开的冰山图，决定通过或退回修改。</p></article>
        <article class="rule-panel"><h3 class="text-info">内容整理 CONTENT_CURATION</h3><p>维护精选和主题入口，但不能借此修改作者正文。</p></article>
        <article class="rule-panel"><h3 class="text-warning">社区管理 COMMUNITY_MODERATION</h3><p>处理举报、警告、下架和申诉。</p></article>
        <article class="rule-panel"><h3 class="text-danger">站点管理 SITE_ADMINISTRATION</h3><p>维护系统设置、处理权限申请和紧急情况。</p></article>
      </div>
      <article class="rule-panel mt-3">
        <h3>发布审核员资格</h3>
        <div class="mt-3 grid gap-3 text-xs leading-6 text-text-body sm:grid-cols-2">
          <ul>
            <li>注册满 30 天。</li>
            <li>最近 180 天至少 5 个已合并 PR。</li>
            <li>贡献涉及至少 3 张不同冰山图。</li>
            <li>至少完成 3 次非本人 PR 审阅。</li>
            <li>最近 180 天无严重警告。</li>
          </ul>
          <ul>
            <li>认证后进入 30 天试用，每日最多作出 3 次发布决定。</li>
            <li>试用期至少完成 10 次审核，复查问题率低于 10%，可以转为正式。</li>
            <li>可以审核自己的作品，但每次都会交给其他人复查。</li>
            <li>试用期抽查 30%，正式审核员抽查 10%。</li>
          </ul>
        </div>
      </article>
      <article class="rule-panel mt-3">
        <h3>双人复核与紧急暂停</h3>
        <p>重要权限的授予和永久撤销需要两名站点管理员确认。一个管理员只能先暂停 72 小时；如果没有第二人确认，权限会自动恢复。管理员人数不足时，创始人可以紧急处理，但必须写明理由。</p>
      </article>
    </section>

    <section id="contributions" class="rule-section">
      <div class="rule-heading">
        <span>04</span>
        <div><h2>参与记录、排行榜与成就</h2><p>不同类型的贡献分开看，不再压成一个总分。</p></div>
      </div>
      <div class="rule-grid sm:grid-cols-2">
        <article class="rule-panel">
          <h3>参与记录</h3>
          <p>作品发布、PR 合并、有效审阅或项目任务完成后，系统会记下一次记录。个人主页会分别显示创作、协作、审阅和项目参与情况。</p>
        </article>
        <article class="rule-panel">
          <h3>分类排行榜</h3>
          <p>只统计已经发布、合并或确认有效的记录，并遵守用户的隐私设置。各类榜单分开计算，不设综合总榜。</p>
        </article>
        <article class="rule-panel">
          <h3>新版成就</h3>
          <p>阅读、收藏、创作、探索和彩蛋成就照常保留；新的协作成就会根据实际合并、审阅和项目任务解锁。</p>
        </article>
        <article class="rule-panel">
          <h3>旧版质量分</h3>
          <p>质量分和 ScoreLog 已冻结，不再新增。本人仍可在个人主页“旧版记录”中查看历史；公开页面不再显示综合等级或质量分总排名。</p>
        </article>
      </div>
      <p class="rule-note mt-3">已经拿到的成就不会收回。依赖旧质量分或旧身份的成就不再新增，但仍会显示在原用户的主页中。</p>
    </section>

    <section id="governance" class="rule-section">
      <div class="rule-heading">
        <span>05</span>
        <div><h2>申请、复查与申诉</h2><p>每次处理都要说明原因，不能只看声望或投票。</p></div>
      </div>
      <div class="rule-grid sm:grid-cols-2">
        <article class="rule-panel">
          <h3>站务申请</h3>
          <p>申请人需要说明相关经验，管理员再结合实际参与记录作出决定。拒绝、暂停、撤销和恢复都会留下原因和操作人。</p>
        </article>
        <article class="rule-panel">
          <h3>抽查与申诉</h3>
          <p>一部分发布审核会交给其他人复查。用户对暂停或处罚有异议时，可以申诉，并由没有参与原决定的人处理。</p>
        </article>
        <article class="rule-panel">
          <h3>账号状态</h3>
          <p>账号状态、冰山图角色、专题角色和站务权限分别计算。账号被限制时，原有站务权限也会暂时停止使用。</p>
        </article>
        <article class="rule-panel">
          <h3>隐私与可见性</h3>
          <p>访客只能看到已经公开的内容和相关合并记录。正在修改的分支、未发布版本和个人草稿只对参与者开放。</p>
        </article>
      </div>
    </section>

    <section id="legacy" class="rule-section">
      <div class="rule-heading">
        <span>06</span>
        <div><h2>旧治理历史</h2><p>保留记录，但不再以旧制度授予权限。</p></div>
      </div>
      <div class="rule-callout">
        <p>旧 RfA、自动晋升、选举、弹劾和质量分已经停止更新。以前的申请、结果和成就都会保留，相关页面只供查看。</p>
        <div class="mt-3 flex flex-wrap gap-2 font-mono text-[10px]">
          <a class="rule-chip" href="/rfa">RfA 档案</a>
          <a class="rule-chip" href="/elections">选举档案</a>
          <a class="rule-chip" href="/impeach">弹劾档案</a>
          <a class="rule-chip" href="/org">当前社区职责</a>
        </div>
      </div>
    </section>
  </main>
</Layout>

<style>
  .rule-section { scroll-margin-top: 6rem; margin-top: 3rem; }
  .rule-heading { display: flex; align-items: flex-start; gap: 1rem; margin-bottom: 1rem; }
  .rule-heading > span { display: grid; width: 2rem; height: 2rem; flex: none; place-items: center; border: 1px solid color-mix(in srgb, var(--brand) 36%, transparent); border-radius: .55rem; color: var(--brand); font: 600 .68rem/1 var(--font-mono); background: color-mix(in srgb, var(--brand) 8%, transparent); }
  .rule-heading h2 { color: var(--text-hi); font: 600 1.05rem/1.5 var(--font-mono); }
  .rule-heading p { margin-top: .2rem; color: var(--text-mid); font-size: .75rem; line-height: 1.5; }
  .rule-grid { display: grid; gap: .75rem; }
  .rule-panel, .rule-callout { border: 1px solid var(--border); border-radius: .75rem; background: color-mix(in srgb, var(--surface-1) 94%, transparent); padding: 1rem; }
  .rule-panel h3 { margin-bottom: .45rem; color: var(--text-hi); font: 600 .75rem/1.5 var(--font-mono); }
  .rule-panel p, .rule-panel li, .rule-callout p { color: var(--text-body); font-size: .75rem; line-height: 1.75; }
  .rule-panel ul { display: grid; gap: .3rem; }
  .rule-panel li { position: relative; padding-left: 1rem; break-inside: avoid; }
  .rule-panel li::before { position: absolute; left: 0; color: var(--brand); content: "·"; }
  .rule-callout { border-color: color-mix(in srgb, var(--brand) 24%, var(--border)); background: linear-gradient(135deg, color-mix(in srgb, var(--brand) 8%, var(--surface-1)), var(--surface-1)); }
  .rule-note { border-left: 2px solid var(--brand); padding: .75rem 1rem; color: var(--text-mid); background: var(--surface-2); font-size: .72rem; line-height: 1.7; }
  .rule-chip { border: 1px solid var(--border); border-radius: .45rem; padding: .45rem .65rem; color: var(--text-mid); background: var(--surface-1); transition: border-color .15s ease, color .15s ease, background .15s ease; }
  .rule-chip:hover { border-color: color-mix(in srgb, var(--brand) 45%, var(--border)); color: var(--brand); background: color-mix(in srgb, var(--brand) 7%, var(--surface-1)); }
  @media (min-width: 640px) {
    .rule-grid.sm\:grid-cols-2 { grid-template-columns: repeat(2, minmax(0, 1fr)); }
    .rule-grid.sm\:grid-cols-3 { grid-template-columns: repeat(3, minmax(0, 1fr)); }
  }
  @supports not (color: color-mix(in srgb, white, black)) {
    .rule-heading > span, .rule-callout, .rule-chip:hover { border-color: var(--border); background: var(--surface-2); }
  }
</style>
