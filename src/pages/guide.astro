---
/**
 * /guide — 平台使用指南
 */
import Layout from '../layouts/Layout.astro';
import { renderMarkdownWithMath } from '../lib/markdown';

const latexInlineExampleSource = '质能等价 $E = mc^2$ 由爱因斯坦提出';
const latexBlockExampleSource = '$$\n\\int_{-\\infty}^{\\infty} e^{-x^2} \\, dx = \\sqrt{\\pi}\n$$';
const latexInlineExampleHtml = renderMarkdownWithMath(latexInlineExampleSource);
const latexBlockExampleHtml = renderMarkdownWithMath(latexBlockExampleSource);
---

<Layout title="平台使用指南 — 冰山图宇宙">
  <main class="max-w-4xl mx-auto px-4 py-8">

    <!-- 标题 -->
    <div class="mb-10">
      <div class="text-[10px] font-mono text-text-mid tracking-widest mb-2">// GUIDE · 平台使用指南</div>
      <h1 class="text-2xl font-mono text-text-hi mb-2">冰山图宇宙 · 平台使用指南</h1>
      <p class="text-xs font-mono text-text-body mb-3">从注册到创作，快速掌握冰山图宇宙的所有功能。</p>

      <!-- 写作指南入口 -->
      <a href="/guide/writing" class="inline-block border border-brand/30 bg-brand/5 px-4 py-2.5 mb-3 hover:border-brand/50 transition-colors">
        <div class="flex items-center gap-2">
          <span class="text-brand font-mono text-xs">&#x270E;</span>
          <span class="text-xs font-mono text-text-hi">查看冰山撰写指南</span>
          <span class="text-[10px] font-mono text-text-mid ml-auto">/guide/writing →</span>
        </div>
        <p class="text-[10px] font-mono text-text-body mt-1">撰写细则、层级规范、词条规范、Markdown 与公式语法 —— 详见撰写指南。</p>
      </a>

      <!-- 跳转索引 -->
      <div class="mt-4 flex flex-wrap gap-x-4 gap-y-1">
        {[
          ['#what',    '什么是冰山图'],
          ['#start',   '快速开始'],
          ['#create',  '创建冰山图'],
          ['#version-control', '版本协作'],
          ['#project', '专题协作'],
          ['#ideas',   '创意板'],
          ['#features','其他功能'],
          ['#faq',     '常见问题'],
        ].map(([href, label]) => (
          <a href={href} class="text-[10px] font-mono text-text-body hover:text-brand transition-colors">
            › {label}
          </a>
        ))}
      </div>
    </div>

    <!-- ── 01 什么是冰山图 ── -->
    <section id="what" class="mb-10">
      <div class="flex items-center gap-2 mb-4">
        <span class="text-[10px] font-mono text-brand tracking-widest">01 · 什么是冰山图</span>
        <div class="flex-1 border-t border-border-minimal"></div>
      </div>

      <div class="border border-border-minimal bg-surface-2 p-4 mb-3">
        <p class="text-[11px] font-mono text-text-body leading-relaxed mb-3">冰山图是一种将某一主题的知识按「大众认知 → 深层奥秘」分层呈现的结构化内容形式。就像冰山一样，浮出水面的只是表层，而真正庞大而有趣的部分藏在水面之下。</p>
        <p class="text-[11px] font-mono text-text-body leading-relaxed mb-3">例如，一张关于「宇宙」的冰山图，顶层可能是「太阳系」「银河系」这样的常识；往下则是「暗物质」「弦理论」；更深处是「时间的本质」「多宇宙猜想」……每一层都比上一层更冷门、更深入、更令人震惊。</p>
        <p class="text-[11px] font-mono text-text-body leading-relaxed">本平台围绕冰山图构建了一个完整的内容社区 —— 你不仅可以浏览和创作冰山图，还能通过 <strong class="text-text-hi">主题门户</strong> 按 35 个分类探索内容、在 <strong class="text-text-hi">精选区</strong> 发现高质量作品、通过 <strong class="text-text-hi">排行榜</strong> 了解社区热门趋势，以及参与 <strong class="text-text-hi">专题协作</strong> 与他人共同策划大型主题。</p>
      </div>

      <!-- 视觉结构示意 -->
      <div class="border border-border-subtle bg-surface-0 p-4 font-mono text-xs">
        <div class="text-[10px] text-text-mid mb-3">// 结构示意</div>
        <div class="space-y-1">
          <div class="flex items-center gap-3">
            <span class="text-text-body w-20 text-right text-[10px]">第 1 层</span>
            <div class="flex-1 bg-brand/10 border border-brand/20 px-3 py-1 text-text-body text-[11px]">
              ██████████████████  大众熟知的常识与入门知识
            </div>
          </div>
          <div class="flex items-center gap-3">
            <span class="text-text-body w-20 text-right text-[10px]">第 2 层</span>
            <div class="flex-1 bg-[#22c55e]/8 border border-success/15 px-3 py-1 text-text-body text-[11px]">
              ██████████████  略有了解的人才知道的内容
            </div>
          </div>
          <div class="flex items-center gap-3">
            <span class="text-text-body w-20 text-right text-[10px]">第 3 层</span>
            <div class="flex-1 bg-[#3b82f6]/8 border border-[#3b82f6]/15 px-3 py-1 text-text-body text-[11px]">
              ██████████  需要深入了解才能接触到的知识
            </div>
          </div>
          <div class="flex items-center gap-3">
            <span class="text-text-body w-20 text-right text-[10px]">第 4 层+</span>
            <div class="flex-1 bg-[#8b5cf6]/8 border border-[#8b5cf6]/15 px-3 py-1 text-text-body text-[11px]">
              ████████  极小众的冷知识、猎奇内容、深层理论
            </div>
          </div>
        </div>
        <div class="mt-3 text-[10px] text-text-body text-center tracking-wider">▼ 越往下越冷门、越小众</div>
      </div>
    </section>

    <!-- ── 02 快速开始 ── -->
    <section id="start" class="mb-10">
      <div class="flex items-center gap-2 mb-4">
        <span class="text-[10px] font-mono text-brand tracking-widest">02 · 快速开始</span>
        <div class="flex-1 border-t border-border-minimal"></div>
      </div>

      <div class="space-y-2">
        {[
          {
            step: '注册 / 登录',
            desc: '点击右上角「登录」，支持 GitHub 账号或邮箱注册。新账号默认为 USER 角色，登录后解锁投票、评论、收藏、创建冰山图等全部互动功能。',
            color: '#30363d',
          },
          {
            step: '浏览内容',
            desc: '前往 <strong class="text-text-hi">冰山广场</strong>（/iceberg/list）浏览所有已发布的冰山图，可按分类、主题、关键词筛选。通过 <strong class="text-text-hi">主题门户</strong>（/topic）按 35 个分类浏览内容，每个主题有独立页面展示该分类下的作品和创意。查看 <strong class="text-text-hi">精选内容</strong>（/featured）发现管理员推荐的高质量作品，或通过 <strong class="text-text-hi">排行榜</strong>（/leaderboard）了解热门趋势。也可从 <strong class="text-text-hi">功能大厅</strong>（/sitemap）浏览所有可用页面。',
            color: '#30363d',
          },
          {
            step: '发现内容',
            desc: '在冰山广场可使用 <strong class="text-text-hi">随机浏览</strong> 功能碰运气发现冷门佳作。按 <strong class="text-text-hi">Ctrl+K</strong>（Mac 为 ⌘K）打开全局搜索，输入至少 2 个关键字即可搜索已发布的冰山图标题。在详情页中可按标签筛选过滤特定类型的词条。',
            color: '#30363d',
          },
          {
            step: '投票、评论与关注',
            desc: '登录后可对冰山图 <strong class="text-text-hi">投赞（+1）或踩（-1）</strong>，改变投票方向会抵消上一次投票。可在词条详情中 <strong class="text-text-hi">发表评论</strong> 并对评论点赞，支持楼中楼回复。在他人主页点击「+ 关注」即可 <strong class="text-text-hi">关注作者</strong>，首页将显示其更新动态。互动记录不会转换成权限。',
            color: '#30363d',
          },
          {
            step: '创建你的第一张冰山图',
            desc: '点击导航栏「创建」按钮进入编辑器，也可从个人主页「+ 创建冰山图」按钮进入。填写标题、摘要、层级与词条，选择主题分类和词条标签后，通过提交前检查单即可提交审核。退出编辑器时会自动保存草稿。',
            color: '#00FF41',
          },
        ].map(({ step, desc, color }, i) => (
          <div class="border border-border-minimal bg-surface-2 p-4 flex gap-4">
            <span class="font-mono text-sm flex-shrink-0 w-6 text-right" style={`color:${color}`}>{i + 1}.</span>
            <div>
              <div class="text-xs font-mono text-text-hi mb-1">{step}</div>
              <p class="text-[11px] font-mono text-text-body leading-relaxed" set:html={desc}></p>
            </div>
          </div>
        ))}
      </div>
    </section>

    <!-- ── 03 创建冰山图 ── -->
    <section id="create" class="mb-10">
      <div class="flex items-center gap-2 mb-4">
        <span class="text-[10px] font-mono text-brand tracking-widest">03 · 创建冰山图</span>
        <div class="flex-1 border-t border-border-minimal"></div>
      </div>

      <div class="space-y-3">

        <div class="border border-border-minimal bg-surface-2 p-4 mb-3 text-[11px] font-mono text-text-body">
          <div class="text-[10px] text-brand mb-3">// 从 Iceberg Threads 导入或同步</div>
          <p class="leading-relaxed mb-3">
            <strong class="text-text-hi">创建新冰山图：</strong>
            在新建编辑器中点击「导入」，粘贴 icebergthreads.com 上的冰山图链接，系统会解析层级和词条结构（每层最多 500 条），导入后可继续修改和完善。词条自身的 NSFW 标记、原图分类和标记会自动转换为本站标签。
          </p>
          <p class="leading-relaxed mb-3">
            <strong class="text-text-hi">更新已经导入的冰山图：</strong>
            打开原冰山图的编辑器，等待当前修改保存完成后点击「同步源站」。获取并预览最新内容，再回到编辑器点击「备份并应用同步」即可。
          </p>
          <div class="border border-brand/20 bg-brand/5 px-3 py-2 text-[10px] leading-relaxed text-text-mid">
            同步采用安全合并：同名层级和词条会更新，源站新增内容会加入；本站独有层级、词条、标签和已有词条 ID 会保留，源站删除不会在本站执行。写入前会生成版本备份并检查协作版本，检测到其他协作者的新修改时整次同步取消，不会只写入一部分。
          </div>
        </div>

        <div class="border border-purple/25 bg-purple/5 p-4 mb-3 text-[11px] font-mono text-text-body">
          <div class="text-[10px] text-purple mb-3">// 多人修改时怎样保存</div>
          <p class="leading-relaxed mb-3">
            编辑器会先把内容自动保存到你自己的<strong class="text-text-hi">工作区</strong>，不会立刻影响其他人。改完一组内容后，点击「提交版本」并写一句说明，其他协作者才能看到并审阅这次修改。
          </p>
          <p class="leading-relaxed mb-3">
            作者和维护者可以直接更新<strong class="text-text-hi">主版本</strong>；其他贡献者要先点击「开始改动」，完成后发起合并请求（Pull Request）。维护者可以查看前后差异、留言、批准，或者要求继续修改。
          </p>
          <div class="border border-purple/20 bg-surface-2/60 px-3 py-2 text-[10px] leading-relaxed text-text-mid">
            如果两个人改的是不同地方，网站会尽量自动合并；如果改到了同一处，会把两个版本并排显示，请你选择保留哪一个。合并后的内容仍是草稿，需要通过发布审核才会公开。
          </div>
        </div>

        <!-- 字段说明 -->
        <div class="border border-border-minimal bg-surface-2 p-4">
          <div class="text-[10px] font-mono text-text-body mb-3">字段说明</div>
          <div class="space-y-3 text-[11px] font-mono">
            <div class="flex gap-3 items-start">
              <span class="text-brand w-14 flex-shrink-0">标题</span>
              <span class="text-text-body">冰山图的主题名称。至少 <strong class="text-text-hi">5 个字</strong>，简洁说明主题即可，例如「日本近代史」「深海生物演化」「量子力学入门」。</span>
            </div>
            <div class="flex gap-3 items-start">
              <span class="text-brand w-14 flex-shrink-0">摘要</span>
              <span class="text-text-body">对冰山图主题的简短介绍。至少 <strong class="text-text-hi">20 个字</strong>，帮助读者了解该冰山图的范围与视角。</span>
            </div>
            <div class="flex gap-3 items-start">
              <span class="text-brand w-14 flex-shrink-0">主题</span>
              <span class="text-text-body">从 35 种主题分类中选择一个，如「历史」「科学」「哲学」「游戏」等。主题分类帮助读者按兴趣浏览内容，也用于 /topic 门户页的索引。</span>
            </div>
            <div class="flex gap-3 items-start">
              <span class="text-brand w-14 flex-shrink-0">层级</span>
              <span class="text-text-body">代表知识深度的分层。至少需要 <strong class="text-text-hi">3 层</strong>，可自定义层名（如「水面」「浅水区」「深水区」「深渊」）。关于层级设计和命名的详细规范，请参阅撰写指南。</span>
            </div>
            <div class="flex gap-3 items-start">
              <span class="text-brand w-14 flex-shrink-0">词条</span>
              <span class="text-text-body">每个知识点。每层至少 <strong class="text-text-hi">2 个词条</strong>，词条有标题（必填）和描述（推荐填写，全图 50% 以上词条须有描述）。词条数少于 5 个将被标记为 🌱 小作品。词条撰写规范详见 <a href="/guide/writing" class="text-brand hover:underline border-b border-brand/30">撰写指南 →</a></span>
            </div>
            <div class="flex gap-3 items-start">
              <span class="text-brand w-14 flex-shrink-0">标签</span>
              <span class="text-text-body">给词条打上分类标签，平台提供 <strong class="text-text-hi">169 种标签</strong>（分标记/内容/来源三大类），编辑器中按类别以 emoji 折叠展开。含成人内容须打 <strong class="text-danger">NSFW</strong> 标签并勾选确认。</span>
            </div>
          </div>
        </div>

        <!-- 提交检查单 -->
        <div class="border border-brand/15 bg-brand/3 p-4">
          <div class="text-[10px] font-mono text-brand mb-3">// 提交前检查单（所有项目必须通过）</div>
          <ul class="space-y-1.5 text-[11px] font-mono">
            {[
              '标题已填写（≥ 5 字）',
              '摘要已填写（≥ 20 字）',
              '已选择主题分类',
              '层数 ≥ 3 层',
              '每层词条数 ≥ 2 条',
              '词条描述覆盖率 ≥ 50%（超过一半的词条有描述）',
              '含 NSFW 内容时已勾选确认',
            ].map(item => (
              <li class="flex items-center gap-2">
                <span class="text-brand">✓</span>
                <span class="text-text-body">{item}</span>
              </li>
            ))}
          </ul>
          <p class="text-[10px] font-mono text-text-mid mt-3">关于层级梯度、词条描述质量标准、NSFW 规则等撰写细则，请参阅 <a href="/guide/writing" class="text-brand hover:underline border-b border-brand/30">冰山撰写指南</a>。</p>
        </div>

        <!-- 草稿与审核流程 -->
        <div class="border border-border-minimal bg-surface-2 p-4">
          <div class="text-[10px] font-mono text-text-body mb-3">草稿保存与审核流程</div>
          <div class="space-y-3 text-[11px] font-mono text-text-body">
            <p>新建冰山图时退出编辑器会自动保存草稿（仅自己可见）。启用版本控制后，自动保存只写入个人工作副本；需要点击「提交版本」才能形成团队可审阅的正式版本。</p>
            <p>完成编辑后点击提交，通过检查单即进入审核队列：</p>
            <div class="flex items-center gap-2 flex-wrap">
              <span class="border border-border-minimal px-2 py-1 text-text-body">草稿</span>
              <span class="text-text-body">──通过检查单──▶</span>
              <span class="border border-warning/20 px-2 py-1 text-warning">待审核</span>
              <span class="text-text-body">──发布审核员──▶</span>
              <span class="border border-[#22c55e30] px-2 py-1 text-success">已发布</span>
            </div>
            <p class="text-[10px] text-text-mid">
              · 审核通常在 24–48 小时内完成。NSFW 内容进入专项审核队列，审核时间可能更长。<br/>
              · 被驳回时会收到通知并附上驳回原因，修改后可重新提交。<br/>
              · 从 Iceberg Threads 同步得到的改动会先进入当前工作副本，并在提交时进入同一版本链；旧版浏览器本地历史仍保留为本机恢复备份。<br/>
              · 编辑器支持 <strong class="text-text-hi">Ctrl+S</strong> 快速保存草稿，<strong class="text-text-hi">Ctrl+Enter</strong> 提交审核。
            </p>
          </div>
        </div>

        <div class="border border-border-minimal bg-surface-2 p-4 text-[11px] font-mono text-text-body">
          <div class="text-[10px] text-brand mb-3">// 一次改动如何进入公开页面</div>
          <div class="flex flex-wrap items-center gap-2 leading-relaxed">
            <span class="border border-border px-2 py-1">工作副本</span>
            <span class="text-text-lo">→</span>
            <span class="border border-purple/30 px-2 py-1 text-purple">提交版本</span>
            <span class="text-text-lo">→</span>
            <span class="border border-info/30 px-2 py-1 text-info">合并到 main</span>
            <span class="text-text-lo">→</span>
            <span class="border border-warning/30 px-2 py-1 text-warning">提交审核</span>
            <span class="text-text-lo">→</span>
            <span class="border border-success/30 px-2 py-1 text-success">公开发布</span>
          </div>
        </div>

      </div>
    </section>

    <!-- ── 03B 版本协作 ── -->
    <section id="version-control" class="mb-10 scroll-mt-24">
      <div class="flex items-center gap-2 mb-4">
        <span class="text-[10px] font-mono text-purple tracking-widest">03B · 问题反馈与版本协作（Issue / Branch / Pull Request）</span>
        <div class="flex-1 border-t border-border-minimal"></div>
      </div>

      <div class="border border-purple/25 bg-purple/5 p-4 mb-3 text-[11px] font-mono text-text-body">
        <p class="leading-relaxed">这套协作方式和 GitHub 有些相似，但不需要安装 Git。你照常在网页里编辑，网站负责记住每次提交、显示前后差异，并在多人改到同一处时提醒你处理。</p>
      </div>

      <div class="grid grid-cols-1 sm:grid-cols-2 gap-2 mb-3 text-[11px] font-mono">
        {[
          ['问题 · Issue', '发现错误或有建议时先发起讨论。Issue 不会创建分支，也不会直接改动冰山图。'],
          ['工作副本 · Working Copy', '编辑器自动保存的地方。每个人各自保存，不会因为别人同时编辑而被覆盖。'],
          ['提交 · Commit', '一次写明作者、时间和说明的正式保存。恢复旧内容时也会留下记录，不会偷偷抹掉历史。'],
          ['分支 · Branch', '一块独立的改稿空间。你可以放心修改，不会直接动到主版本和公开页面。'],
          ['合并请求 · Pull Request', '改完后交给维护者审阅。页面会列出改动，也可以在具体位置留言。'],
        ].map(([title, desc]) => (
          <div class="border border-border-minimal bg-surface-2 p-4">
            <div class="text-text-hi mb-1.5">{title}</div>
            <p class="text-text-body leading-relaxed">{desc}</p>
          </div>
        ))}
      </div>

      <div class="border border-warning/25 bg-warning/5 p-4 mb-3 text-[11px] font-mono">
        <div class="text-warning mb-1.5">什么时候提 Issue，什么时候开始改动？</div>
        <p class="leading-relaxed text-text-body">
          还不确定怎么改、只想报告错误或补充线索时，选择「提出 Issue」。已经准备直接修改内容时，选择「开始改动」创建分支；完成后再发起合并请求。
        </p>
      </div>

      <div class="space-y-2 mb-3 text-[11px] font-mono">
        <div class="border border-border-minimal bg-surface-2 p-4">
          <div class="text-text-hi mb-2">作者或维护者：直接维护主版本</div>
          <ol class="space-y-1 text-text-body">
            <li><span class="text-purple mr-2">1.</span>打开主版本编辑，内容会自动保存。</li>
            <li><span class="text-purple mr-2">2.</span>改完后点击「提交版本」，简单写清这次改了什么。</li>
            <li><span class="text-purple mr-2">3.</span>需要公开时再点击「提交审核」。送审以后继续修改，也不会混进正在审核的那一版。</li>
          </ol>
        </div>
        <div class="border border-border-minimal bg-surface-2 p-4">
          <div class="text-text-hi mb-2">贡献者：通过分支与合并请求参与</div>
          <ol class="space-y-1 text-text-body">
            <li><span class="text-info mr-2">1.</span>点击「开始改动」，建立自己的改稿分支。</li>
            <li><span class="text-info mr-2">2.</span>边改边保存；一时写不完也没关系，可以分几次提交。</li>
            <li><span class="text-info mr-2">3.</span>发起合并请求，告诉维护者这次改了什么、为什么改。</li>
            <li><span class="text-info mr-2">4.</span>如果维护者提出意见，继续在原分支修改并重新提交。</li>
          </ol>
        </div>
      </div>

      <div class="grid grid-cols-1 lg:grid-cols-3 gap-2 mb-3 text-[11px] font-mono">
        <div class="border border-success/25 bg-success/5 p-4">
          <div class="text-success mb-1.5">自动合并</div>
          <p class="text-text-body leading-relaxed">两个人改的是不同词条或不同位置时，网站通常可以自动合在一起，不用手动复制粘贴。</p>
        </div>
        <div class="border border-warning/25 bg-warning/5 p-4">
          <div class="text-warning mb-1.5">冲突解决</div>
          <p class="text-text-body leading-relaxed">如果两个人改了同一段文字，或者一人删除、一人继续编辑，网站会显示两个版本，请协作者决定保留哪一个。</p>
        </div>
        <div class="border border-info/25 bg-info/5 p-4">
          <div class="text-info mb-1.5">发布隔离</div>
          <p class="text-text-body leading-relaxed">个人改稿、尚未合并的分支和未发布内容只对参与者可见。普通访客看到的仍是上一次审核通过的版本。</p>
        </div>
      </div>

      <div class="border border-border-minimal bg-surface-2 p-4 text-[11px] font-mono">
        <div class="text-text-hi mb-2">权限如何确定？</div>
        <div class="space-y-1.5 text-text-body leading-relaxed">
          <p><span class="text-brand">个人冰山图：</span>作者是维护者；作者可以单独邀请 CONTRIBUTOR 或 MAINTAINER。</p>
          <p><span class="text-brand">Projects 冰山图：</span>普通项目成员自动成为贡献者；项目创建者与项目管理员自动成为维护者，无需为每张图重复邀请。</p>
          <p><span class="text-brand">站务权限：</span>负责发布审核或社区管理，并不代表可以修改别人的冰山图。</p>
          <p><span class="text-brand">外部投稿：</span>作者开放投稿后，登录用户可以提交自己的修改，但不能直接写进主版本。</p>
        </div>
        <p class="mt-3 text-[10px] text-text-mid">如果某张图尚未启用版本控制，它会继续使用原有编辑流程；管理员会通过功能开关和回填校验分阶段启用，不会直接改写生产数据。</p>
      </div>
    </section>

    <!-- ── 04 专题协作 ── -->
    <section id="project" class="mb-10">
      <div class="flex items-center gap-2 mb-4">
        <span class="text-[10px] font-mono text-[#f97316] tracking-widest">04 · 专题协作（WikiProject）</span>
        <div class="flex-1 border-t border-border-minimal"></div>
      </div>

      <div class="border border-border-minimal bg-surface-2 p-4 mb-3 text-[11px] font-mono text-text-body">
        <p class="leading-relaxed mb-3">专题适合需要多人长期整理的大选题。你可以用看板分任务，在讨论区商量方向，再把相关冰山图放到同一个专题里。普通成员负责改稿，项目管理员负责审阅和合并。</p>
        <p class="leading-relaxed">前往 <a href="/projects" class="text-brand hover:underline border-b border-brand/30">/projects</a> 浏览现有项目或创建新项目。</p>
      </div>

      <div class="space-y-2">
        {[
          {
            icon: '+', label: '创建与加入项目',
            desc: '点击「创建专题」设置名称、简介和主题分类。当前活跃专题允许已登录用户直接加入；项目管理员也可以复制带有加入参数的邀请链接，分享给希望参与的用户。',
          },
          {
            icon: '▣', label: '任务看板',
            desc: '每个项目内置看板（Kanban），按「待处理 / 进行中 / 已完成」三列管理任务。成员可创建任务卡片，设置描述、优先级、截止时间和负责人，并用任务卡上的状态按钮推进流程。',
          },
          {
            icon: '◈', label: '成员管理',
            desc: '专题分为创建者、管理员和普通成员。创建者与管理员可以审阅和合并，普通成员可以提交修改；离开专题后，对应的编辑权限也会取消。',
          },
          {
            icon: '▸', label: '协作编辑',
            desc: '把冰山图加入专题后，成员可以各自在自己的分支上修改，再交给管理员审阅。创意板里的选题也可以加入专题，方便成员认领。',
          },
          {
            icon: '◎', label: '讨论区',
            desc: '讨论区适合商量选题、分工和整体方向。针对某次改稿的意见，写在合并请求里会更容易对应到具体内容。',
          },
          {
            icon: '⎉', label: '邀请链接',
            desc: '项目管理员可以复制专题加入链接。链接只负责把已登录用户带入当前活跃专题；冰山图写入权限仍由项目角色、分支保护和合并审阅共同约束。',
          },
        ].map(({ icon, label, desc }, i) => (
          <div class="border border-border-minimal bg-surface-2 p-4 flex gap-4">
            <span class="font-mono text-xs text-text-mid mt-0.5 w-4 flex-shrink-0 text-center">{icon}</span>
            <div>
              <div class="text-xs font-mono text-text-hi mb-1">{label}</div>
              <p class="text-[11px] font-mono text-text-body leading-relaxed" set:html={desc}></p>
            </div>
          </div>
        ))}
      </div>
    </section>

    <!-- ── 05 创意板 ── -->
    <section id="ideas" class="mb-10">
      <div class="flex items-center gap-2 mb-4">
        <span class="text-[10px] font-mono text-info tracking-widest">05 · 创意板</span>
        <div class="flex-1 border-t border-border-minimal"></div>
      </div>

      <div class="border border-border-minimal bg-surface-2 p-4 mb-3 text-[11px] font-mono text-text-body">
        <p class="leading-relaxed mb-3">创意板是一个社区协作空间，任何人都可以在这里提交冰山图创意、Bug 反馈或功能建议，供其他创作者认领和实现。它像是社区的「需求池」——有想法但没时间做？提交创意让其他人来实现。想创作但缺灵感？浏览创意板找到你感兴趣的主题。</p>
        <p class="leading-relaxed">前往 <a href="/ideas" class="text-brand hover:underline border-b border-brand/30">/ideas</a> 浏览创意或提交新点子。</p>
      </div>

      <div class="space-y-2">
        {[
          {
            icon: '◉', label: '看板视图与列表视图',
            desc: '创意板支持两种浏览模式：<strong class="text-text-hi">看板视图</strong> 按「待认领 / 已认领 / 已完成」三列分组展示，类似项目管理的 Kanban 风格；<strong class="text-text-hi">列表视图</strong> 按时间线排列所有创意，适合快速浏览最新提交。可通过页面切换按钮随时切换视图。',
          },
          {
            icon: '⊞', label: '主题筛选与排序',
            desc: '左侧可折叠主题筛选器列出所有 35 个分类，点击即可只查看特定主题的创意。支持按最新、最热、最多认领等排序方式切换，方便快速找到感兴趣的内容。',
          },
          {
            icon: '▣', label: '三种提交模板',
            desc: '提交创意时可选择三种模板：<strong class="text-text-hi">🧊 冰山创意</strong>（提交冰山图选题建议，可附主题分类和简要描述）、<strong class="text-text-hi">🐛 Bug 反馈</strong>（报告平台问题，含复现步骤和严重程度）、<strong class="text-text-hi">💡 功能建议</strong>（提出新功能需求，说明使用场景）。',
          },
          {
            icon: '?', label: '认领与实现',
            desc: '看到感兴趣的创意？点击「认领」按钮即可将其标记为「已认领」，其他人看到后就不会重复认领。完成创作后将冰山图链接到创意条目，再标记为「已完成」即可。一个创意可被多人认领，鼓励多个版本百花齐放。',
          },
          {
            icon: '#', label: '短 ID 与评论区引用',
            desc: '每个创意自动获得一个短 ID（如 <strong class="text-text-hi">#1a3f</strong>），在评论区使用 <code class="text-text-hi">#1a3f</code> 即可引用该创意。点击短 ID 可快速跳转到对应的创意详情。讨论区支持 Markdown 格式和 @ 提及，方便围绕创意展开深入讨论。',
          },
        ].map(({ icon, label, desc }, i) => (
          <div class="border border-border-minimal bg-surface-2 p-4 flex gap-4">
            <span class="font-mono text-xs text-text-mid mt-0.5 w-4 flex-shrink-0 text-center">{icon}</span>
            <div>
              <div class="text-xs font-mono text-text-hi mb-1">{label}</div>
              <p class="text-[11px] font-mono text-text-body leading-relaxed" set:html={desc}></p>
            </div>
          </div>
        ))}
      </div>
    </section>

    <!-- ── 06 其他功能 ── -->
    <section id="features" class="mb-10">
      <div class="flex items-center gap-2 mb-4">
        <span class="text-[10px] font-mono text-purple tracking-widest">06 · 其他功能</span>
        <div class="flex-1 border-t border-border-minimal"></div>
      </div>

      <div class="space-y-2">
        {[
          {
            icon: '⬛', label: '冰山广场',
            link: '/iceberg/list', linkLabel: '/iceberg/list',
            desc: '浏览所有已发布的冰山图，支持按分类、主题、关键词搜索。点击卡片展开各层词条与描述。发布后作品自动出现在广场中。',
          },
          {
            icon: '◉', label: '主题门户',
            link: '/topic', linkLabel: '/topic',
            desc: '按 35 种主题分类浏览冰山图和创意。每个主题有独立页面（/topic/[slug]），显示该主题下的冰山图、创意和统计数据，支持按状态和排序筛选。',
          },
          {
            icon: '★', label: '精选内容',
            link: '/featured', linkLabel: '/featured',
            desc: '由管理员精选的高质量冰山图合集。首页以轮播卡片形式展示（最多 12 张），精选作品在广场和详情页有金色特殊标识，ADMIN 可在详情页切换精选状态。',
          },
          {
            icon: '▲', label: '排行榜',
            link: '/leaderboard', linkLabel: '/leaderboard',
            desc: '查看平台内容与用户排行。顶部三强卡片英雄区展示冠亚季军（金银铜边框），三个标签页切换 <strong class="text-text-hi">热门/高赞/用户</strong> 榜单，支持全部/月/周时间范围筛选，点击条目直达对应冰山图或用户主页。',
          },
          {
            icon: '▸', label: '冰山详情 Infobox',
            link: null, linkLabel: null,
            desc: '冰山详情页右上角可折叠信息抽屉，展示作者、层级数、词条总数、浏览量、是否精选、小作品标记、关联创意等元数据，一键收起不打扰阅读。',
          },
          {
            icon: '↓', label: '导出为图片',
            link: null, linkLabel: null,
            desc: '在已发布冰山图详情页点击「导出为图片」生成 PNG 信息图。导图含 # 前缀标题、元数据副标题、加粗色条、页脚 URL 水印和二维码，支持浅色/深色主题切换。词条 chip 附带标签小圆点，颜色与标签一致，一目了然。',
          },
          {
            icon: '♡', label: '收藏与收藏夹',
            link: null, linkLabel: null,
            desc: '登录后可收藏感兴趣的冰山图，在个人主页的「收藏」标签页中集中查看和管理。收藏内容按时间排序，支持取消收藏。',
          },
          {
            icon: '◈', label: '关注与动态流',
            link: null, linkLabel: null,
            desc: '在他人主页点击「+ 关注」即可关注作者。关注后首页显示其更新动态，/api/feed 可获取关注流。取消关注即可停止接收动态。',
          },
          {
            icon: '🔔', label: '通知中心',
            link: '/notifications', linkLabel: '/notifications',
            desc: '导航栏铃铛图标显示未读数字角标。点开可预览最近通知（审核结果、评论回复、投票动态等），支持单条已读、一键全部已读，以及「查看全部通知」进入完整通知列表页。',
          },
          {
            icon: '⚙', label: '首页快捷入口',
            link: null, linkLabel: null,
            desc: '首页最上方可自定义 16 个快捷链接。点击 ⚙ 按钮打开配置面板，勾选常用功能后自动排列，配置保存在浏览器本地存储中。',
          },
          {
            icon: '▚', label: 'CRT 扫描线效果',
            link: null, linkLabel: null,
            desc: '可选的复古 CRT 扫描线视觉效果。在导航栏用户下拉菜单中一键开关，为页面叠加经典显示器扫描线纹理（浅色模式下自动隐藏）。',
          },
          {
            icon: '▤', label: '阅读进度',
            link: null, linkLabel: null,
            desc: '登录后浏览冰山图词条时，页面顶部显示阅读进度条。已读词条数在元数据区展示，下次打开时恢复上次阅读位置。',
          },
          {
            icon: '▥', label: '词条搜索与弹窗',
            link: null, linkLabel: null,
            desc: '冰山详情页支持搜索过滤词条，点击词条弹出居中弹窗查看完整 Markdown 描述。←→ 键切换前后词条，Esc 关闭弹窗。',
          },
          {
            icon: '◆', label: '个人主页',
            link: null, linkLabel: null,
            desc: '查看目前承担的站务、参与记录、作品、收藏和成就。旧质量分只在本人可见的「旧版记录」中保留，公开页面不再显示综合等级。',
          },
          {
            icon: '⬙', label: '用户框',
            link: null, linkLabel: null,
            desc: '个人主页展示的装饰性标签。在「设置 → 用户框定制」中选择，从多类别预设框中挑选，默认最多 3 个。解锁更多社区成就可扩展至 6 个，成就拥有稀有度等级（普通/稀有/史诗/传说），稀有度越高越难得。部分框需特定成就才能选用。',
          },
          {
            icon: '◉', label: '小作品（Stub）标记',
            link: null, linkLabel: null,
            desc: '自动评估冰山图质量：词条数 < 5 显示 🌱 标记，词条数 < 10 且描述覆盖率 < 30% 显示 🔧 标记，提示读者该作品尚待完善，欢迎作者继续补充内容。',
          },
          {
            icon: '☰', label: '草稿系统',
            link: null, linkLabel: null,
            desc: '新建冰山图时退出编辑器会自动保存草稿。草稿仅自己可见，可在个人主页「草稿」Tab 中继续编辑或删除。',
          },
          {
            icon: '!', label: '举报',
            link: null, linkLabel: null,
            desc: '在冰山图或评论处点击举报并填写原因。社区管理员会查看相关内容和记录，必要时先将内容下架。',
          },
          {
            icon: '↑', label: '申请参与站务',
            link: '/org', linkLabel: '/org',
            desc: '想参与发布审核、内容整理或社区管理，可以申请对应站务。发布审核员也可以凭实际协作和审阅记录进入试用；成就和旧身份不会自动带来权限。',
          },
          {
            icon: '⊘', label: '申诉',
            link: null, linkLabel: null,
            desc: '账号受到处罚或站务权限被暂停后，可以提交申诉并查看处理结果。申诉会交给没有参与原决定的人复查。',
          },
          {
            icon: '+', label: '功能大厅',
            link: '/sitemap', linkLabel: '/sitemap',
            desc: '统一的功能入口页，列出所有可用页面和功能模块。从导航栏「功能」下拉或 Footer 链接进入。',
          },
          {
            icon: '{}', label: 'API 文档',
            link: '/api-docs', linkLabel: '/api-docs',
            desc: '面向开发者的公开 API 参考文档，列出冰山图、评论、搜索、收藏等所有公开接口签名。',
          },
          {
            icon: '↩', label: '撤销/重做',
            link: null, linkLabel: null,
            desc: '编辑器中支持 Ctrl+Z 撤销、Ctrl+Y 重做，最多保存 80 步操作历史。添加/修改/删除/移动词条或层级均自动记录快照，误操作一键回退。',
          },
          {
            icon: '⚡', label: '性能优化',
            link: null, linkLabel: null,
            desc: '大型冰山图（1000+ 词条）加载已全面优化：描述按需异步加载、屏幕外层级延迟渲染、描述预渲染到数据库。页面体积缩小 95%，滚动流畅无卡顿。',
          },
          {
            icon: '🌐', label: 'SEO & 分享',
            link: null, linkLabel: null,
            desc: '全站支持 Open Graph 和 Twitter Card 分享预览卡片；每张冰山图嵌入 JSON-LD 结构化数据；已提交 sitemap.xml 到各大搜索引擎。分享链接到社交平台时自动展示标题、描述和预览图。',
          },
        ].map(({ icon, label, link, linkLabel, desc }) => (
          <div class="border border-border-minimal bg-surface-2 px-4 py-3 flex gap-3 items-start">
            <span class="text-xs font-mono text-text-mid mt-0.5 w-4 flex-shrink-0">{icon}</span>
            <div class="flex-1 min-w-0">
              <div class="flex items-center gap-2 mb-1 flex-wrap">
                <span class="text-xs font-mono text-text-hi">{label}</span>
                {link && (
                  <a href={link} class="text-[10px] font-mono text-text-mid hover:text-brand transition-colors">{linkLabel}</a>
                )}
              </div>
              <p class="text-[11px] font-mono text-text-body leading-relaxed" set:html={desc}></p>
            </div>
          </div>
        ))}
      </div>
    </section>

    <!-- ── 07 常见问题 ── -->
    <section id="faq" class="mb-10">
      <div class="flex items-center gap-2 mb-4">
        <span class="text-[10px] font-mono text-success tracking-widest">07 · 常见问题</span>
        <div class="flex-1 border-t border-border-minimal"></div>
      </div>

      <div class="space-y-2" id="faq-list">
        {[
          {
            q: '冰山图和百科有什么区别？',
            a: '百科追求面面俱到的中立记述，而冰山图按「认知深度」分层组织知识——顶层是大众常识，底层是极小众的冷门内容。冰山图更适合呈现一个主题的「探索感」，让读者从熟悉走向未知。两者并不互斥，冰山图描述中可以引用百科来源。',
          },
          {
            q: '我的冰山图提交后多久会被审核？',
            a: '一般 24–48 小时内由发布审核员处理。平台活跃度较低时可能稍长。含 NSFW 内容的会进入专项队列，时间不定。',
          },
          {
            q: '冰山图被驳回后我该怎么做？',
            a: '你会收到站内通知，并附有驳回原因。根据原因修改草稿（尤其是层级结构、描述质量、NSFW 标签）后，可重新提交审核。',
          },
          {
            q: '如何把 Iceberg Threads 的更新同步到现有冰山图？',
            a: '进入这张冰山图的编辑器，等待当前修改保存后点击「同步源站」，粘贴或确认原始链接并获取数据。预览无误后返回编辑器，点击「备份并应用同步」。系统只更新同名内容并加入新增内容，不会因为源站删除而删除本站内容；同步前也会生成可恢复的版本备份。',
          },
          {
            q: '什么样的内容会被驳回？',
            a: '无事实依据的内容、层级梯度不合理（上层常识和底层内容区分不明显）、词条描述覆盖率低于 50%、NSFW 内容未打标、含广告/垃圾信息、与主题无关、重复提交相似主题等情况均可能被驳回。',
          },
          {
            q: '我能修改已发布的冰山图吗？',
            a: '发布后可以继续编辑词条内容，但对结构的重大修改需要重新提交审核。具体逻辑以编辑器的提示为准。',
          },
          {
            q: '质量分和成就会影响权限吗？',
            a: '不会。旧质量分只保留为本人可见的历史，成就也只是纪念。能否处理站务，要看当前获得的站务权限；能否修改某张冰山图，则要看你在该作品或专题中的身份。',
          },
          {
            q: '参与记录是怎么计算的？',
            a: '作品真正发布、PR 合并、审阅确认有效或项目任务完成后，系统才会记一次。个人主页会把创作、协作、审阅和项目参与分开显示，不再合成一个总分。',
          },
          {
            q: '原来的 RfA、晋升和选举去哪里了？',
            a: '旧 RfA、自动晋升、选举和弹劾已经停用，原页面只保留历史记录。现在想参与站务，需要申请具体工作；发布审核员也可以在满足协作条件后进入试用。详情见平台规则。',
          },
          {
            q: '小作品（Stub）标记是什么？',
            a: '词条数少于 5 个显示 🌱，词条数少于 10 个且描述覆盖率低于 30% 显示 🔧。这是自动评估标记，提示该作品内容尚浅，欢迎作者继续完善。',
          },
          {
            q: '如何找到感兴趣的主题？',
            a: '推荐三种方式：① 访问 <strong class="text-text-hi">/topic 主题门户</strong>，按 35 种分类浏览冰山图和创意，每个主题有独立页面展示该分类下的所有内容；② 在冰山广场使用 <strong class="text-text-hi">Ctrl+K 全局搜索</strong> 按关键字查找；③ 使用广场的 <strong class="text-text-hi">随机浏览</strong> 功能和排行榜发现热门或冷门佳作。',
          },
          {
            q: '如何参与专题协作？',
            a: '前往 <strong class="text-text-hi">/projects</strong> 浏览活跃专题。加入后可以认领任务、参与讨论，并修改专题里的冰山图。普通成员提交改稿，项目创建者和管理员负责审阅与合并。只有冰山图作者或站点管理员可以把作品加入专题。',
          },
          {
            q: '自动保存和提交版本有什么区别？',
            a: '自动保存只是防止刷新、断网或误关页面造成内容丢失，其他人不会立刻看到。点击「提交版本」并填写说明后，这次改稿才会进入协作记录，供其他人比较和审阅。',
          },
          {
            q: '合并请求通过后为什么公开页面还没变化？',
            a: '合并请求只会更新主草稿，不会直接改公开页面。维护者还要点击「提交审核」，审核通过后读者才会看到新版。送审以后继续修改，也不会混进正在审核的那一版。',
          },
          {
            q: '多人同时修改时还会互相覆盖吗？',
            a: '不会直接覆盖。两个人改的是不同地方时，网站会尝试自动合并；如果改到同一处，页面会把两个版本列出来，请你选择保留哪一个。同一账号开多个标签页时，也会提示较新的保存记录。',
          },
          {
            q: '词条标签有什么用？',
            a: '平台提供 <strong class="text-text-hi">169 种标签</strong>（标记/内容/来源三大类），用于标注词条的属性。标签帮助读者按类型筛选过滤词条（如在详情页只看 NSFW 警示内容或只看学术来源内容），也增强了内容的可发现性。编辑器中标签按 emoji 类别折叠展开，使用方便。漏打 NSFW 标签会导致冰山图被驳回。',
          },
          {
            q: '搜索功能怎么用？',
            a: '点击导航栏搜索框或按 Ctrl+K（Mac 上是 ⌘K）打开全局搜索，输入至少 2 个字即可搜索已发布的冰山图标题。在冰山详情页内也可搜索过滤当前作品的词条。',
          },
          {
            q: '如何反馈 bug 或建议？',
            a: '前往反馈页（/feedback）提交问题或建议，管理员会查看并回复。也可在创意板上以「Bug 反馈」或「功能建议」模板提交，供社区讨论。',
          },
        ].map(({ q, a }, i) => (
          <div class="border border-border-minimal bg-surface-2 overflow-hidden">
            <button
              type="button"
              class="w-full text-left px-4 py-3 flex items-start gap-2 hover:bg-[#0f131a] transition-colors faq-btn"
              data-index={i}
            >
              <span class="text-brand font-mono text-xs mt-0.5 flex-shrink-0">?</span>
              <span class="text-xs font-mono text-text-body leading-snug">{q}</span>
              <span class="ml-auto text-text-mid font-mono text-xs flex-shrink-0 faq-arrow">+</span>
            </button>
            <div class="faq-body px-4 pb-0 max-h-0 overflow-hidden transition-all duration-200">
              <p class="text-[11px] font-mono text-text-body leading-relaxed pb-3 border-t border-border-minimal pt-2" set:html={a}></p>
            </div>
          </div>
        ))}
      </div>
    </section>

    <!-- 底部链接 -->
    <div class="flex items-center justify-between flex-wrap gap-4 border-t border-border-minimal pt-6">
      <div class="flex gap-4">
        <a href="/guide/writing" class="text-[10px] font-mono text-text-mid hover:text-brand transition-colors">
          查看撰写指南 →
        </a>
        <a href="/rules" class="text-[10px] font-mono text-text-mid hover:text-brand transition-colors">
          查看完整规则 →
        </a>
        <a href="/iceberg/list" class="text-[10px] font-mono text-text-mid hover:text-brand transition-colors">
          前往冰山广场 →
        </a>
      </div>
      <a href="/feedback" class="text-[10px] font-mono text-text-mid hover:text-brand transition-colors">
        反馈与建议 →
      </a>
    </div>

  </main>
</Layout>

<script>
  // FAQ 折叠/展开
  document.querySelectorAll('.faq-btn').forEach(btn => {
    btn.addEventListener('click', () => {
      const body = btn.nextElementSibling as HTMLElement;
      const arrow = btn.querySelector('.faq-arrow') as HTMLElement;
      const isOpen = body.style.maxHeight && body.style.maxHeight !== '0px';
      if (isOpen) {
        body.style.maxHeight = '0px';
        body.style.paddingBottom = '0';
        if (arrow) arrow.textContent = '+';
      } else {
        body.style.maxHeight = body.scrollHeight + 'px';
        if (arrow) arrow.textContent = '−';
      }
    });
  });
</script>
